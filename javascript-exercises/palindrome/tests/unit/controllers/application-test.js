import { moduleFor, test } from 'ember-qunit';

moduleFor('controller:application', 'controllers/application-test.js', {
});

test('it exists', function () {
    var controller = this.subject();
    ok(controller);
});
